{% extends 'base.html.twig' %}

{% block title %}Autores{% endblock %}

{% block body %}
    <h1>Autores</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ autore.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ autore.title }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ autore.author }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('autores_index') }}">back to list</a>

    <a href="{{ path('autores_edit', {'id': autore.id}) }}">edit</a>

    {{ include('autores/_delete_form.html.twig') }}
{% endblock %}
